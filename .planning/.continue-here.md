---
context: research
phase: phase_4_real_world_pricing
status: paused_at_natural_break
last_updated: 2026-06-23T10:34:05Z
linkedin_published: true
---

# Quick Resume Guide

You paused on **2026-06-23** after a productive session that:
1. Hit the wall of real options pricing (Phase 4 kickoff)
2. Built the full automation infrastructure for live paper-trading
3. Published the LinkedIn post
4. Queued mentor-trades research (Path C) for next session

This is a **research project**, not a GSD phase-based project — no phase/plan/task structure. Resume by reading the next-actions section below.

---

## Critical Anti-Patterns (do NOT repeat)

| Pattern | What happened | Severity | Mitigation |
|---|---|---|---|
| Constant-vol BS overstates collectable credit | Backtest assumed $200 ES R1 credit; real market gave $150 with much worse R:R | **blocking** | Always validate against real market quotes before committing capital. Use `decision_today.py` with real `--market_credit_usd` before any TAKE decision. |
| "$2,000 cap by structure" framing | Brief implied spread structure caps loss at $2K; actually 8% of theoretical max-losses exceed $2K (NQ M1 worst was -$1,284) | advisory | Be specific: "single-contract sizing caps loss to ~$2K for most cells; NQ M1 and NKD M1 can exceed it" |
| Multiple comparisons understated | "One cell stood out" — actual search space was 60+ cells, not 6 | advisory | Stratification narrative should say "the entire mid-VIX × gap_down ROW was clean across all 5 symbols + both alignment cells" |
| Spending Databento without permission | Per user instruction (2026-06-23) | **blocking** | Free-data scripts (yfinance, cached) for daily ops. Databento only after explicit user approval each time. ~$67 of $80 credits remaining. |

---

## Current State

Phase 4 (Real-World Pricing) kicked off following today's discovery that BS-constant-vol overestimates collectable credit.

- Both R1 candidate trades (ES + NQ) FIRED at signal scan → IBKR real quotes showed R:R 46:1 (NQ) and 12:1 (ES modified) → **both SKIPPED**. Logged to `pricing_log.csv`.
- Strategic realization: R1's deep-OTM short collects almost no real credit. Mentor's near-ATM strikes likely the real edge.
- Path 3 (live decision layer) built: `paper_trading/decision_today.py`
- Path 1 (empirical haircut) deferred until ≥10 quoted samples accumulate via cron
- Daily automation: cron-ready, macOS notify + optional ntfy.sh phone push

LinkedIn post published 2026-06-23.

---

## Required Reading (in order, when resuming)

1. `notebooks/11_strategy_brief.html` — the public-facing summary that went live
2. `.planning/HANDOFF.json` — full project state, all rule specs, decisions log
3. `paper_trading/CRON_SETUP.md` — automation setup instructions
4. `paper_trading/pricing_log.csv` — 3 entries from 2026-06-23 (all SKIP)
5. `charts/mentor trades/` — 4 screenshots for next-session Path C research:
   - `PHOTO-2026-06-15-19-50-55.jpg`
   - `PHOTO-2026-06-18-21-14-08.jpg`
   - `WhatsApp Image 2026-06-22 at 17.28.57.jpeg`
   - `a280915b-bdba-4afb-b320-7cce0c1ca7b1.jpg`
6. `social/linkedin_post.md` — published copy

---

## Infrastructure State

| Component | Status | Action needed |
|---|---|---|
| LinkedIn post | **PUBLISHED** 2026-06-23 | Monitor comments |
| Cron jobs | Built, NOT installed | User: `crontab -e` per `CRON_SETUP.md` |
| macOS Full Disk Access for cron | NOT granted | Settings → Privacy → add `/usr/sbin/cron` |
| ntfy.sh phone push | Optional, not configured | Install app + `NTFY_TOPIC` in `.env` |
| Local Databento data | Last fetched ES through 2026-06-19 RTH | Stale; refresh needs permission |
| `paper_trading/trades.csv` | Empty (no live trades) | Populates as user takes signals |
| `paper_trading/pricing_log.csv` | 3 entries (today's SKIPs) | Baseline empirical sample |

---

## Decisions Made This Session

1. **Never spend Databento credits without explicit user permission** (codified in `HANDOFF.json:operational_rules`). Cost-checks via `metadata.get_cost` are free and OK.
2. **Skip R1 trades failing live thresholds** (default `R:R ≤ 8:1` AND `market PoP ≥ 88%`). Today's 2 SKIPs validated by these.
3. **Path 1 deferred** in favor of live-sample accumulation via cron. Cheaper, more representative.
4. **RTY excluded** from production universe (already in brief, per OOS).
5. **Path C (mentor's edge) is the next active research direction**, not more Phase 4 model refinement.

---

## Open Questions Carried Forward

1. **What's the mentor's actual decision mechanism?** 4 screenshots available. Working hypothesis: enters overnight Globex (~5am ET / 12pm Israel), strikes near today's open, exits ~20 min after US RTH open.
2. **Does R1 survive if K_short moves from prev_close to (prev_close − 0.25×ATR)?** Phase-4 candidate to test. Requires options chain data → needs permission.
3. **Does empirical haircut from live samples confirm or contradict the model?** Needs ~10 quoted candidates (weeks).
4. **Will LinkedIn audience push back on data-mining?** Defenses: walk-forward chart + "row not cell" framing.

---

## Next Session — Start Here

**Priority order:**

1. **(15 min) Read the 4 mentor screenshots** in `charts/mentor trades/`. Decode each: timestamp, ES/NQ price at entry, strikes chosen, structure, outcome if shown.
2. **(20 min) Find the common pattern**: time-of-day, strike-distance-from-spot, expiry, exit timing. Hypothesis to test: "enters 4-5am ET on overnight Globex, near-ATM strikes, exits ~20 min after US RTH open."
3. **(30 min) Build a candidate "mentor rule" backtestable spec.** Trigger condition, strike anchor formula, exit rule.
4. **Decide whether to ask user for Databento options-chain budget** if the candidate rule looks promising.

**Background:**
- Review LinkedIn comments + respond
- Check whether cron was installed (`crontab -l`)
- Check `pricing_log.csv` for new entries from daily scans

---

## Context (mental model)

Transition point: **public-facing Phase 1-3 work is shipped. Phase 4 research begins.** Path B (live accumulation) runs automatically. Path C (mentor edge) is active focus.

Critical insight: **the model overestimates the strategy's real-world economics because the entire "deep-OTM short" architecture is the wrong place to sell premium.** The mentor's success suggests near-ATM is where real edge lives — and the gap-signal tells you *when* to play that game, not "sell tail insurance."

If mentor analysis validates this, Phase 4 stops being "fix R1's pricing model" and becomes "redesign the strategy around the mentor's strike-selection." Big pivot.

---

<next_action>
Open the 4 mentor screenshots in `charts/mentor trades/`. Decode them in order of timestamp (June 15, 18, 22, and the unnamed UUID).
</next_action>
